import { AnswerLoader } from "@/components/AnswerLoader";
import { ThemeProvider } from "@/components/theme-provider";

function App() {
  return (
    <ThemeProvider>
      <AnswerLoader />
    </ThemeProvider>
  );
}

export default App;
